---
generated_at: 2026-06-19
generator: /flow:estimate
context: whole
source_input: ./docs/concept.md
interpreted_as:
  feature: null
  phase: rough
  source: autodetect
phase: rough
confidence_band:
  ai_impl: "±300%"
  human_bottleneck: "±50%"
  ai_tokens: "±50%"
nfr_profile:
  scale: medium
  throughput: medium
  latency: standard
  availability: 99
  source: default
calibration:
  global_samples: 0
  pj_samples: 0
  note: "global-metrics.jsonl 空・PJ STATS なし → デフォルト係数。band は rough 既定。"
summary:
  min:
    total_files: 40
    total_lines: 3200
    human_hours: 6
  standard:
    total_files: 95
    total_lines: 7500
    human_hours: 12
  full:
    total_files: 190
    total_lines: 15000
    human_hours: 22
---

# 見積もり: AIと一緒に描くマインドマップ（全体・初回フェルミ推定）

> **phase=rough**（concept のみ、SPEC 未生成）。confidence band は広い（AI-impl ±300%）。
> Phase 2 で最初の 1 feature 完了後に `refined` で再キャリブレーションする。
> **金額換算は利用側に委ねる**（コード行数 × AI コーディングレート / トークン × モデル単価）。

## サマリ表

| スコープ | ファイル数 | コード行数 (logic+test) | 人間時間 | AI 推論トークン (設計+実装) |
|---|---|---|---|---|
| Minimum | 40 | 3,200 | 6h | 設計 150K / 実装 350K = 約 500K (±50%) |
| Standard | 95 | 7,500 | 12h | 設計 344K / 実装 1.0M = 約 1.35M (±50%) |
| Full | 190 | 15,000 | 22h | 設計 600K / 実装 2.2M = 約 2.8M (±50%) |

> **NFR プロファイル**: scale=medium, throughput=medium, latency=standard, availability=99（**source=default、想定値です**）
> **キャリブレーション**: global-metrics.jsonl 空のためデフォルト係数。実装が進めば metrics が蓄積され精度向上。

## 1. 横断フォルダ別ブレークダウン（Standard 基準、logic+test）

| フォルダ | classification | Std files | Std lines | 根拠 |
|---|---|---|---|---|
| _shared/db | ai_impl | 6 | 400 | Neon 5 テーブル(maps/nodes/edges/usage_log/users) + Drizzle スキーマ + マイグレーション |
| _shared/types | ai_impl | 4 | 200 | MindMapNode/Edge/MapMeta/AiSuggestion/UsageEvent |
| _shared/auth | ai_impl | 6 | 500 | Clerk 匿名ゲスト→段階認証 + withOwner 所有者リゾルバ（SEC-001）+ データ引き継ぎ |
| _shared/ai-client | ai_impl | 5 | 450 | OpenAI ラッパ + store=false + PII scrub(SEC-003) + 構造化 JSON + リトライ |
| _shared/cost-tracking | ai_impl | 4 | 350 | usage_log 積算 + .env 単価 + 無料枠アラート（§4.6.2） |
| _shared/ui | ai_impl | 8 | 400 | shadcn/ui テーマ + 基本コンポーネント + レイアウトシェル |
| _shared/app-shell | ai_impl | 8 | 600 | 合成ルート + router + providers + API ルートハンドラ + Clerk セッション + deploy scaffold（O57） |
| **横断小計** | | **41** | **2,900** | |

## 2. 機能フォルダ別ブレークダウン（Standard 基準、logic+test）

| フォルダ | classification | Std files | Std lines | 根拠 |
|---|---|---|---|---|
| live-capture | ai_impl | 5 | 450 | Web Speech API フック + テキスト入力 + ストリーミングバッファ |
| mindmap-canvas | ai_impl | 8 | 900 | React Flow キャンバス + カスタムノード + 手編集(追加/削除/並替/つなぎ直し) + レイアウト（最重量） |
| map-management | ai_impl | 5 | 400 | マップ CRUD + 一覧 + リネーム/削除（所有者スコープ SEC-001） |
| ai-structuring | ai_impl | 6 | 700 | **コア**: 逐次マージロジック + 構造化プロンプト + ツリー diff/merge + prompt injection 耐性(SEC-002) |
| ai-expand | ai_impl | 5 | 450 | 枝提案(関連/対立/問い/具体例) + 観点補完 + 提案 UI |
| export | ai_impl | 5 | 400 | 画像/Markdown/アウトライン変換 + CSV/MD injection エスケープ(SEC-002) |
| billing | ai_impl | 5 | 400 | Stripe PWYW + Webhook 署名検証 + 無料枠/追加枠管理 |
| feedback | ai_impl | 3 | 200 | 👍/👎 + バグ報告ウィジェット + PII scrub + hub ingestion |
| legal | ai_impl | 5 | 350 | /legal/* ページ + consent + データ削除セルフサービス(O54) |
| **機能小計** | | **47** | **4,250** | |

## 3. 基本部分（greenfield、最小構成）

| 項目 | Std files | Std lines | 根拠 |
|---|---|---|---|
| PJ 初期化 + CI/CD + 監視(Sentry)設定 + dev スクリプト | 7 | 350 | Vite/Vercel/GitHub Actions/Sentry/scripts/dev.sh。個人ツールは最小構成 + レート制限(SEC-004 Upstash) |
| **基本小計** | **7** | **350** | |

**Standard 合計**: 41 + 47 + 7 = **95 files / 7,500 lines**

## 4. NFR 倍率の効き
| NFR 軸 | 値 | 倍率 |
|---|---|---|
| scale | medium | 1.0x |
| throughput | medium | 1.0x |
| latency | standard | 1.0x |
| availability | 99 | 1.0x |

合計 NFR 倍率: 1.0x（個人ツール無料枠想定、default プロファイル）

## 5. Human-bottleneck（人間時間）
E2E は基本自動化（Playwright headless）。自動化で代替不可の人力のみ計上:
- Web Speech API 実機テスト（マイク/ブラウザ依存、自動化困難）: ~3h
- ライブ逐次マージ UX の目視・体感確認: ~3h
- Stripe live 課金スモーク（B-4 まとめ確認）: ~2h
- デザインレビュー（/flow:design 視覚レビュー）: ~2h
- 法務文面の最終確認: ~2h

Standard 合計 ~12h（±50%）

## 6. AI 推論トークン（フェルミ推定、Standard）
- **設計**: concept 13K + feature/cross 設計 ×16 ≈ 160K + spec-review ×16 ≈ 80K + secure(design+deps) 18K + audit(light×2+full) 59K + estimate 14K ≈ **344K**
- **実装**: tdd 平均 ~60K/エリア ×16 + e2e ≈ **約 1.0M**
- **合計 ≈ 1.35M tokens（±50%）**
> 単価換算は時期で変動するため参考のみ（金額換算は利用側に委ねる）。

## 7. 二重計上回避
全フォルダ新規（greenfield）。is_new=true。横断既存設計の再計上なし。

## 注意点
- **phase=rough**: concept のみが入力。SPEC 生成後に大きく変動し得る（特に mindmap-canvas / ai-structuring のコア複雑度）
- **最大の不確実性**: [論点-002] ライブ逐次マージの「矛盾なくマージ」ロジック（ai-structuring）。差別化の核心ゆえ実装難度が読みにくく、Full 寄りに振れる可能性
- **NFR=default**: 個人ツール無料枠想定。利用増で scale 上昇すれば倍率が効く
- Phase 2 で最初の 1 feature（推奨: _shared/db）完了後に `/flow:estimate` 再実行で refined に校正
